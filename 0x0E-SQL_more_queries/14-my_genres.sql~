-- lists all genres
SELECT DISTINCT tv_genres.name
FROM tv_genres
INNER JOIN (tv_show_genres NATURAL JOIN tv_shows)
ON tv_genres.id = tv_show_genres.genre_id
WHERE tv_shows.title = 'Dexter'
ORDER BY tv_genres.name;
